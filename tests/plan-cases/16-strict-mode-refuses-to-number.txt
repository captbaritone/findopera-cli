The same tree as 13. Numbering is the designed fallback, so by default it is a
note and the plan can be acted on; under `--require-variants` the caller has
said every duplicate must be named, so each one is spelled out.

--- template
{{opera.title}} \[{{id}}\][ ({{variant}})]
--- tree
rips/a/332.txt
rips/b/332.txt
elsewhere/75.txt
--- strict
yes
--- listing
elsewhere	Billy Budd [75]
rips/a	Don Giovanni [332] (1)
rips/b	Don Giovanni [332] (2)
--- report
2 directories would take the same name and were numbered by walk order. Write a word into each marker instead — one you choose, which `{{variant}}` then picks up:
    mv 'rips/a/332.txt' 'rips/a/332 <word>.txt'
    mv 'rips/b/332.txt' 'rips/b/332 <word>.txt'
