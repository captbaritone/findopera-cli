332 and 5876 are separate FindOpera recordings of the same performance: same
year, same conductor, same singers. Without the id in the template they are
indistinguishable, and no variant was declared, so they are numbered.

--- template
{{composer.lastName}}/{{opera.title}}[ ({{variant}})]
--- tree
a/332.txt
b/5876.txt
--- listing
a	Mozart/Don Giovanni (1)
b	Mozart/Don Giovanni (2)
--- report
2 directories were numbered by walk order because no variant was declared; those numbers shift as the library changes. Write a word into each marker to fix them — mv 'a/332.txt' 'a/332 <word>.txt' — or pass --require-variants to make this an error.
