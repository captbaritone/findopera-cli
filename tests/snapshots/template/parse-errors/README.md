# parse-errors

Everything caught before any data is looked at. A template is validated
against the schema at parse time, so a mistyped field fails immediately
rather than on whichever record first exposes it.
