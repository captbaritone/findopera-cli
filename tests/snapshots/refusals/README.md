# When findopera.com will not answer

How a refusal reads. A GraphQL server reports its complaint in the body of a
perfectly successful response, so none of these is an HTTP failure — a client
that looked only at the status would call every one of them a success.

The answers here are written out rather than taken from the captured
recordings, because their subject is a reply nothing real would give.
