# This is the latest

Nothing to say beyond saying so. The answer names `{version}` rather than a
number, so this case does not go wrong at the next release. Exit zero either way: a run that found no
update has not failed.

## Answer

```json
{"tag_name":"v{version}"}
```

## Run

```console
$ findopera self update
```
