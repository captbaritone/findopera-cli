# The same, as JSON

For anything that would rather decide for itself. `how` is the command a
person would be told to run.

## Answer

```json
{"tag_name":"v99.0.0"}
```

## Run

```console
$ findopera self update --json
```
