# Ahead of the latest release

What a build from a checkout looks like. Calling it up to date would be a
small lie, so it says which way round the two are.

## Answer

```json
{"tag_name":"v0.0.1"}
```

## Run

```console
$ findopera self update
```
