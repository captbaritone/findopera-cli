# `findopera delete --help`

What somebody meets before they have run anything. It is the only
documentation most people will read, so it is checked in like any other
output.

## Run

```console
$ findopera delete --help
```
