--- template
{{year}} {{month}}
--- error
error[template_unresolvable]: {{year}} can resolve to nothing, and is not inside a group
{{year}} {{month}}
^^^^^^^^
help: Add a fallback like {{…|"Unknown"}}, or wrap it in a group so it can be dropped: [{{year}}]
