# unicode

Non-ASCII, in the template and in the data. Spans are byte offsets while
underlines are counted in characters, so a caret has to land under the right
character regardless of how many bytes precede it.
