[package]
name = "findopera"
version = "0.1.0"
edition = "2021"
description = "A small template language for rendering record metadata into path-safe names"

[lib]
name = "findopera"
path = "src/lib.rs"

[dependencies]

[profile.release]
strip = true
